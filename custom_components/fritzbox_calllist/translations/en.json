{
  "config": {
    "step": {
      "user": {
        "title": "Set up FRITZ!Box Calllist",
        "description": "Choose your FRITZ!Box call monitor sensor.",
        "data": {
          "name": "Name",
          "callmonitor_entity": "Call monitor sensor",
          "max_items": "Stored entries"
        }
      }
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "FRITZ!Box Calllist options",
        "description": "External reverse lookup is optional. Phone numbers are sent to the configured providers.",
        "data": {
          "reverse_lookup": "Enable external reverse lookup",
          "reverse_lookup_providers": "Provider order, comma-separated"
        }
      }
    }
  }
}
