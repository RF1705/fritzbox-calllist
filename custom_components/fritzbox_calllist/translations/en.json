{
  "config": {
    "step": {
      "user": {
        "title": "Set up FRITZ!Box Calllist",
        "description": "Choose your FRITZ!Box call monitor sensor.",
        "data": {
          "name": "Name",
          "callmonitor_entity": "Call monitor sensor",
          "max_items": "Stored entries"
        }
      }
    }
  },
  "options": {
    "error": {
      "callmonitor_already_configured": "This call monitor sensor is already used by another FRITZ!Box Calllist."
    },
    "step": {
      "init": {
        "title": "Configure FRITZ!Box Calllist",
        "description": "Change the call monitor sensor or manage the reverse lookup cache. Current cache entries: {cache_entries}.",
        "data": {
          "callmonitor_entity": "Call monitor sensor",
          "cache_action": "Cache action"
        }
      }
    }
  }
}
